apply plugin: 'java'
apply plugin: 'groovy'
apply plugin: "net.ltgt.apt"

apply from: 'https://rawgit.com/kkojaeh/pico-erp-gradle/master/test-cfg.gradle'

afterEvaluate {
  dependencies {

    compile project(':audit-impl')

  }
}
